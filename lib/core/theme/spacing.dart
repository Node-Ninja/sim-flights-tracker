import 'package:flutter/cupertino.dart';

class Spacing {

  static const double standardPadding = 20;
  static const double standardMargin = 20;
  static const double halfPadding = 10;
  static const double halfMargin = 10;
  static const double quarterPadding = 5;
  static const double quarterMargin = 5;

  static const EdgeInsets standardPaddingInset = EdgeInsets.all(standardPadding);
  static const EdgeInsets standardMarginInset = EdgeInsets.all(standardMargin);
  static const EdgeInsets halfPaddingInset = EdgeInsets.all(halfPadding);
  static const EdgeInsets halfMarginInset = EdgeInsets.all(halfMargin);
  static const EdgeInsets quarterPaddingInset = EdgeInsets.all(quarterPadding);
  static const EdgeInsets quarterMarginInset = EdgeInsets.all(quarterMargin);

}